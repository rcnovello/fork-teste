version: '3'

services:
  nginx:
    image: nginx
    ports:
      - "8080:8080"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - sites_node

  sites_node:
    build: ./ 
    command: npm start
    ports:
      - "3000:3000"
      - "3001:3001"
